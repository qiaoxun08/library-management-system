<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.SysRolePermissionMapper">

    <select id="findByRoleId" resultType="com.library.entity.SysRolePermission">
        SELECT id, role_id, permission_id, create_time
        FROM sys_role_permission WHERE role_id = #{roleId}
    </select>

    <insert id="insert" parameterType="com.library.entity.SysRolePermission">
        INSERT INTO sys_role_permission (role_id, permission_id)
        VALUES (#{roleId}, #{permissionId})
    </insert>

    <delete id="deleteByRoleId">
        DELETE FROM sys_role_permission WHERE role_id = #{roleId}
    </delete>

    <delete id="deleteByPermissionId">
        DELETE FROM sys_role_permission WHERE permission_id = #{permissionId}
    </delete>

</mapper>
